# Backend API base URL — set this when deploying frontend separately from backend.
# Default: auto-derived from window.location (works for local dev and same-host Docker).
# REACT_APP_API_BASE=https://your-backend-url.com

# Sentry DSN — optional, enables error tracking in production
# REACT_APP_SENTRY_DSN=https://...@sentry.io/...
